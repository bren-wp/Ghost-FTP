name: Retired web workflow

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Web targets retired
        run: |
          echo "The legacy web targets were removed from Ghost FTP."
          echo "No web application is built or published from this repository."
