name: Ghost FTP native build

# RC12 production release gate: only Ghost FTP desktop bundles are eligible for end-user releases.
# RC12 release build stamp: 2026-09-20.12

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'ghostftp-desktop/**'
      - '.github/workflows/ghostftp-build.yml'
  pull_request:
    paths:
      - 'ghostftp-desktop/**'
      - '.github/workflows/ghostftp-build.yml'
      - '.github/workflows/ghostftp-release.yml'

permissions:
  contents: read

concurrency:
  group: ghostftp-native-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            args: '--bundles nsis'
            artifact: 'GhostFTP-Windows-x64-RC12'
          - os: ubuntu-22.04
            args: '--bundles deb,rpm,appimage'
            artifact: 'GhostFTP-Linux-x86_64-RC12'
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: ghostftp-desktop/package-lock.json
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: ghostftp-desktop/src-tauri
      - name: Linux dependencies
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf
      - name: Install frontend dependencies
        working-directory: ghostftp-desktop
        run: npm ci
      - name: TypeScript/frontend build
        working-directory: ghostftp-desktop
        run: npm run build
      - name: Build Ghost FTP desktop bundle
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: ghostftp-desktop
          args: ${{ matrix.args }}
      - name: Capture Windows native-window QA evidence
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $qaDir = Join-Path $env:GITHUB_WORKSPACE 'qa'
          New-Item -ItemType Directory -Force $qaDir | Out-Null
          $exe = Join-Path $env:GITHUB_WORKSPACE 'ghostftp-desktop\src-tauri\target\release\ghostftp.exe'
          if (-not (Test-Path $exe)) { throw "Ghost FTP executable not found: $exe" }

          $proc = Start-Process -FilePath $exe -PassThru
          try {
            $handle = [IntPtr]::Zero
            for ($i = 0; $i -lt 15; $i++) {
              Start-Sleep -Seconds 1
              $proc.Refresh()
              if ($proc.HasExited) { throw "Ghost FTP exited before the QA capture" }
              if ($proc.MainWindowHandle -ne 0) {
                $handle = $proc.MainWindowHandle
                break
              }
            }
            if ($handle -eq [IntPtr]::Zero) { throw "Ghost FTP did not expose a native main-window handle" }
            if ($proc.MainWindowTitle -ne 'Ghost FTP') { throw "Unexpected Ghost FTP window title: $($proc.MainWindowTitle)" }

            Add-Type -AssemblyName System.Drawing
            Add-Type @"
          using System;
          using System.Runtime.InteropServices;
          public static class GhostFtpWindowCapture {
            [StructLayout(LayoutKind.Sequential)]
            public struct RECT { public int Left; public int Top; public int Right; public int Bottom; }
            [DllImport("user32.dll")]
            public static extern bool GetWindowRect(IntPtr hWnd, out RECT rect);
          }
          "@

            $rect = New-Object GhostFtpWindowCapture+RECT
            if (-not [GhostFtpWindowCapture]::GetWindowRect($handle, [ref]$rect)) {
              throw "GetWindowRect failed"
            }
            $width = $rect.Right - $rect.Left
            $height = $rect.Bottom - $rect.Top
            if ($width -lt 480 -or $height -lt 600) {
              throw "Unexpected Ghost FTP window size: ${width}x${height}"
            }

            $bitmap = New-Object System.Drawing.Bitmap $width, $height
            $graphics = [System.Drawing.Graphics]::FromImage($bitmap)
            try {
              $graphics.CopyFromScreen($rect.Left, $rect.Top, 0, 0, $bitmap.Size)
              $bitmap.Save((Join-Path $qaDir 'GhostFTP-Windows-RC12-Native-Window.png'), [System.Drawing.Imaging.ImageFormat]::Png)
            } finally {
              $graphics.Dispose()
              $bitmap.Dispose()
            }

            @(
              "ProcessId=$($proc.Id)"
              "WindowTitle=$($proc.MainWindowTitle)"
              "WindowBounds=${width}x${height}"
              "Executable=$exe"
            ) | Set-Content -Encoding UTF8 (Join-Path $qaDir 'GhostFTP-Windows-RC12-Native-Window.txt')
          } finally {
            if (-not $proc.HasExited) {
              $null = $proc.CloseMainWindow()
              Start-Sleep -Seconds 1
              if (-not $proc.HasExited) { Stop-Process -Id $proc.Id -Force }
            }
          }

      - name: Upload Windows native-window QA evidence
        if: runner.os == 'Windows' && always()
        uses: actions/upload-artifact@v4
        with:
          name: GhostFTP-Windows-x64-RC12-QA
          path: qa/*
          if-no-files-found: warn
          retention-days: 14

      - name: Upload native bundles
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            ghostftp-desktop/src-tauri/target/release/bundle/**
            ghostftp-desktop/src-tauri/Cargo.lock
            ghostftp-desktop/src-tauri/target/release/ghostftp.exe
            ghostftp-desktop/src-tauri/target/release/ghostftp
          if-no-files-found: error

  source-packages:
    name: RC12 source packages
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Package RC12 source and documentation
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          git archive --format=zip --output=dist/GhostFTP-v2.1.1-RC12-Source.zip HEAD
          zip -qr dist/GhostFTP-v2.1.1-RC12-Desktop-Source.zip ghostftp-desktop
          zip -qr dist/GhostFTP-v2.1.1-RC12-Website.zip website
          zip -qr dist/GhostFTP-v2.1.1-RC12-Updates.zip updates
          zip -qr dist/GhostFTP-v2.1.1-RC12-Documentation.zip docs README.md CHANGELOG.md SECURITY.md LICENSE.txt EULA.txt
      - name: Upload RC12 source packages
        uses: actions/upload-artifact@v4
        with:
          name: GhostFTP-RC12-Source-Packages
          path: dist/*
          if-no-files-found: error
          retention-days: 14
