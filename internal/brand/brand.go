package brand

const (
	ProductName = "Ghost FTP"
	ProductFull = "Ghost FTP file transfer client"
	Company     = "Ghost FTP"
	Publisher   = "BRENDIGO LTD"

	// Runtime metadata is intentionally schemeless so the application does not
	// embed a fixed HTTP(S) network destination. About may display only official
	// publisher-owned destinations; repository and issue-tracker URLs belong in
	// documentation/release metadata rather than the runtime product card.
	Website = "brendigo.com"
	Support = "brendigo.com/kontakt"
)
